// Code generated by SQLBoiler 4.15.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// UserDeviceToGeofence is an object representing the database table.
type UserDeviceToGeofence struct {
	UserDeviceID string    `boil:"user_device_id" json:"user_device_id" toml:"user_device_id" yaml:"user_device_id"`
	GeofenceID   string    `boil:"geofence_id" json:"geofence_id" toml:"geofence_id" yaml:"geofence_id"`
	CreatedAt    time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt    time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *userDeviceToGeofenceR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L userDeviceToGeofenceL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var UserDeviceToGeofenceColumns = struct {
	UserDeviceID string
	GeofenceID   string
	CreatedAt    string
	UpdatedAt    string
}{
	UserDeviceID: "user_device_id",
	GeofenceID:   "geofence_id",
	CreatedAt:    "created_at",
	UpdatedAt:    "updated_at",
}

var UserDeviceToGeofenceTableColumns = struct {
	UserDeviceID string
	GeofenceID   string
	CreatedAt    string
	UpdatedAt    string
}{
	UserDeviceID: "user_device_to_geofence.user_device_id",
	GeofenceID:   "user_device_to_geofence.geofence_id",
	CreatedAt:    "user_device_to_geofence.created_at",
	UpdatedAt:    "user_device_to_geofence.updated_at",
}

// Generated where

var UserDeviceToGeofenceWhere = struct {
	UserDeviceID whereHelperstring
	GeofenceID   whereHelperstring
	CreatedAt    whereHelpertime_Time
	UpdatedAt    whereHelpertime_Time
}{
	UserDeviceID: whereHelperstring{field: "\"devices_api\".\"user_device_to_geofence\".\"user_device_id\""},
	GeofenceID:   whereHelperstring{field: "\"devices_api\".\"user_device_to_geofence\".\"geofence_id\""},
	CreatedAt:    whereHelpertime_Time{field: "\"devices_api\".\"user_device_to_geofence\".\"created_at\""},
	UpdatedAt:    whereHelpertime_Time{field: "\"devices_api\".\"user_device_to_geofence\".\"updated_at\""},
}

// UserDeviceToGeofenceRels is where relationship names are stored.
var UserDeviceToGeofenceRels = struct {
	Geofence   string
	UserDevice string
}{
	Geofence:   "Geofence",
	UserDevice: "UserDevice",
}

// userDeviceToGeofenceR is where relationships are stored.
type userDeviceToGeofenceR struct {
	Geofence   *Geofence   `boil:"Geofence" json:"Geofence" toml:"Geofence" yaml:"Geofence"`
	UserDevice *UserDevice `boil:"UserDevice" json:"UserDevice" toml:"UserDevice" yaml:"UserDevice"`
}

// NewStruct creates a new relationship struct
func (*userDeviceToGeofenceR) NewStruct() *userDeviceToGeofenceR {
	return &userDeviceToGeofenceR{}
}

func (r *userDeviceToGeofenceR) GetGeofence() *Geofence {
	if r == nil {
		return nil
	}
	return r.Geofence
}

func (r *userDeviceToGeofenceR) GetUserDevice() *UserDevice {
	if r == nil {
		return nil
	}
	return r.UserDevice
}

// userDeviceToGeofenceL is where Load methods for each relationship are stored.
type userDeviceToGeofenceL struct{}

var (
	userDeviceToGeofenceAllColumns            = []string{"user_device_id", "geofence_id", "created_at", "updated_at"}
	userDeviceToGeofenceColumnsWithoutDefault = []string{"user_device_id", "geofence_id"}
	userDeviceToGeofenceColumnsWithDefault    = []string{"created_at", "updated_at"}
	userDeviceToGeofencePrimaryKeyColumns     = []string{"user_device_id", "geofence_id"}
	userDeviceToGeofenceGeneratedColumns      = []string{}
)

type (
	// UserDeviceToGeofenceSlice is an alias for a slice of pointers to UserDeviceToGeofence.
	// This should almost always be used instead of []UserDeviceToGeofence.
	UserDeviceToGeofenceSlice []*UserDeviceToGeofence
	// UserDeviceToGeofenceHook is the signature for custom UserDeviceToGeofence hook methods
	UserDeviceToGeofenceHook func(context.Context, boil.ContextExecutor, *UserDeviceToGeofence) error

	userDeviceToGeofenceQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	userDeviceToGeofenceType                 = reflect.TypeOf(&UserDeviceToGeofence{})
	userDeviceToGeofenceMapping              = queries.MakeStructMapping(userDeviceToGeofenceType)
	userDeviceToGeofencePrimaryKeyMapping, _ = queries.BindMapping(userDeviceToGeofenceType, userDeviceToGeofenceMapping, userDeviceToGeofencePrimaryKeyColumns)
	userDeviceToGeofenceInsertCacheMut       sync.RWMutex
	userDeviceToGeofenceInsertCache          = make(map[string]insertCache)
	userDeviceToGeofenceUpdateCacheMut       sync.RWMutex
	userDeviceToGeofenceUpdateCache          = make(map[string]updateCache)
	userDeviceToGeofenceUpsertCacheMut       sync.RWMutex
	userDeviceToGeofenceUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var userDeviceToGeofenceAfterSelectHooks []UserDeviceToGeofenceHook

var userDeviceToGeofenceBeforeInsertHooks []UserDeviceToGeofenceHook
var userDeviceToGeofenceAfterInsertHooks []UserDeviceToGeofenceHook

var userDeviceToGeofenceBeforeUpdateHooks []UserDeviceToGeofenceHook
var userDeviceToGeofenceAfterUpdateHooks []UserDeviceToGeofenceHook

var userDeviceToGeofenceBeforeDeleteHooks []UserDeviceToGeofenceHook
var userDeviceToGeofenceAfterDeleteHooks []UserDeviceToGeofenceHook

var userDeviceToGeofenceBeforeUpsertHooks []UserDeviceToGeofenceHook
var userDeviceToGeofenceAfterUpsertHooks []UserDeviceToGeofenceHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *UserDeviceToGeofence) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userDeviceToGeofenceAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *UserDeviceToGeofence) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userDeviceToGeofenceBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *UserDeviceToGeofence) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userDeviceToGeofenceAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *UserDeviceToGeofence) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userDeviceToGeofenceBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *UserDeviceToGeofence) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userDeviceToGeofenceAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *UserDeviceToGeofence) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userDeviceToGeofenceBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *UserDeviceToGeofence) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userDeviceToGeofenceAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *UserDeviceToGeofence) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userDeviceToGeofenceBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *UserDeviceToGeofence) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userDeviceToGeofenceAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddUserDeviceToGeofenceHook registers your hook function for all future operations.
func AddUserDeviceToGeofenceHook(hookPoint boil.HookPoint, userDeviceToGeofenceHook UserDeviceToGeofenceHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		userDeviceToGeofenceAfterSelectHooks = append(userDeviceToGeofenceAfterSelectHooks, userDeviceToGeofenceHook)
	case boil.BeforeInsertHook:
		userDeviceToGeofenceBeforeInsertHooks = append(userDeviceToGeofenceBeforeInsertHooks, userDeviceToGeofenceHook)
	case boil.AfterInsertHook:
		userDeviceToGeofenceAfterInsertHooks = append(userDeviceToGeofenceAfterInsertHooks, userDeviceToGeofenceHook)
	case boil.BeforeUpdateHook:
		userDeviceToGeofenceBeforeUpdateHooks = append(userDeviceToGeofenceBeforeUpdateHooks, userDeviceToGeofenceHook)
	case boil.AfterUpdateHook:
		userDeviceToGeofenceAfterUpdateHooks = append(userDeviceToGeofenceAfterUpdateHooks, userDeviceToGeofenceHook)
	case boil.BeforeDeleteHook:
		userDeviceToGeofenceBeforeDeleteHooks = append(userDeviceToGeofenceBeforeDeleteHooks, userDeviceToGeofenceHook)
	case boil.AfterDeleteHook:
		userDeviceToGeofenceAfterDeleteHooks = append(userDeviceToGeofenceAfterDeleteHooks, userDeviceToGeofenceHook)
	case boil.BeforeUpsertHook:
		userDeviceToGeofenceBeforeUpsertHooks = append(userDeviceToGeofenceBeforeUpsertHooks, userDeviceToGeofenceHook)
	case boil.AfterUpsertHook:
		userDeviceToGeofenceAfterUpsertHooks = append(userDeviceToGeofenceAfterUpsertHooks, userDeviceToGeofenceHook)
	}
}

// One returns a single userDeviceToGeofence record from the query.
func (q userDeviceToGeofenceQuery) One(ctx context.Context, exec boil.ContextExecutor) (*UserDeviceToGeofence, error) {
	o := &UserDeviceToGeofence{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for user_device_to_geofence")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all UserDeviceToGeofence records from the query.
func (q userDeviceToGeofenceQuery) All(ctx context.Context, exec boil.ContextExecutor) (UserDeviceToGeofenceSlice, error) {
	var o []*UserDeviceToGeofence

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to UserDeviceToGeofence slice")
	}

	if len(userDeviceToGeofenceAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all UserDeviceToGeofence records in the query.
func (q userDeviceToGeofenceQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count user_device_to_geofence rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q userDeviceToGeofenceQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if user_device_to_geofence exists")
	}

	return count > 0, nil
}

// Geofence pointed to by the foreign key.
func (o *UserDeviceToGeofence) Geofence(mods ...qm.QueryMod) geofenceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.GeofenceID),
	}

	queryMods = append(queryMods, mods...)

	return Geofences(queryMods...)
}

// UserDevice pointed to by the foreign key.
func (o *UserDeviceToGeofence) UserDevice(mods ...qm.QueryMod) userDeviceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UserDeviceID),
	}

	queryMods = append(queryMods, mods...)

	return UserDevices(queryMods...)
}

// LoadGeofence allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (userDeviceToGeofenceL) LoadGeofence(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUserDeviceToGeofence interface{}, mods queries.Applicator) error {
	var slice []*UserDeviceToGeofence
	var object *UserDeviceToGeofence

	if singular {
		var ok bool
		object, ok = maybeUserDeviceToGeofence.(*UserDeviceToGeofence)
		if !ok {
			object = new(UserDeviceToGeofence)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUserDeviceToGeofence)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUserDeviceToGeofence))
			}
		}
	} else {
		s, ok := maybeUserDeviceToGeofence.(*[]*UserDeviceToGeofence)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUserDeviceToGeofence)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUserDeviceToGeofence))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &userDeviceToGeofenceR{}
		}
		args = append(args, object.GeofenceID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userDeviceToGeofenceR{}
			}

			for _, a := range args {
				if a == obj.GeofenceID {
					continue Outer
				}
			}

			args = append(args, obj.GeofenceID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`devices_api.geofences`),
		qm.WhereIn(`devices_api.geofences.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Geofence")
	}

	var resultSlice []*Geofence
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Geofence")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for geofences")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for geofences")
	}

	if len(geofenceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Geofence = foreign
		if foreign.R == nil {
			foreign.R = &geofenceR{}
		}
		foreign.R.UserDeviceToGeofences = append(foreign.R.UserDeviceToGeofences, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.GeofenceID == foreign.ID {
				local.R.Geofence = foreign
				if foreign.R == nil {
					foreign.R = &geofenceR{}
				}
				foreign.R.UserDeviceToGeofences = append(foreign.R.UserDeviceToGeofences, local)
				break
			}
		}
	}

	return nil
}

// LoadUserDevice allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (userDeviceToGeofenceL) LoadUserDevice(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUserDeviceToGeofence interface{}, mods queries.Applicator) error {
	var slice []*UserDeviceToGeofence
	var object *UserDeviceToGeofence

	if singular {
		var ok bool
		object, ok = maybeUserDeviceToGeofence.(*UserDeviceToGeofence)
		if !ok {
			object = new(UserDeviceToGeofence)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUserDeviceToGeofence)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUserDeviceToGeofence))
			}
		}
	} else {
		s, ok := maybeUserDeviceToGeofence.(*[]*UserDeviceToGeofence)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUserDeviceToGeofence)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUserDeviceToGeofence))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &userDeviceToGeofenceR{}
		}
		args = append(args, object.UserDeviceID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userDeviceToGeofenceR{}
			}

			for _, a := range args {
				if a == obj.UserDeviceID {
					continue Outer
				}
			}

			args = append(args, obj.UserDeviceID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`devices_api.user_devices`),
		qm.WhereIn(`devices_api.user_devices.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load UserDevice")
	}

	var resultSlice []*UserDevice
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice UserDevice")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for user_devices")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for user_devices")
	}

	if len(userDeviceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.UserDevice = foreign
		if foreign.R == nil {
			foreign.R = &userDeviceR{}
		}
		foreign.R.UserDeviceToGeofences = append(foreign.R.UserDeviceToGeofences, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserDeviceID == foreign.ID {
				local.R.UserDevice = foreign
				if foreign.R == nil {
					foreign.R = &userDeviceR{}
				}
				foreign.R.UserDeviceToGeofences = append(foreign.R.UserDeviceToGeofences, local)
				break
			}
		}
	}

	return nil
}

// SetGeofence of the userDeviceToGeofence to the related item.
// Sets o.R.Geofence to related.
// Adds o to related.R.UserDeviceToGeofences.
func (o *UserDeviceToGeofence) SetGeofence(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Geofence) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"devices_api\".\"user_device_to_geofence\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"geofence_id"}),
		strmangle.WhereClause("\"", "\"", 2, userDeviceToGeofencePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.UserDeviceID, o.GeofenceID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.GeofenceID = related.ID
	if o.R == nil {
		o.R = &userDeviceToGeofenceR{
			Geofence: related,
		}
	} else {
		o.R.Geofence = related
	}

	if related.R == nil {
		related.R = &geofenceR{
			UserDeviceToGeofences: UserDeviceToGeofenceSlice{o},
		}
	} else {
		related.R.UserDeviceToGeofences = append(related.R.UserDeviceToGeofences, o)
	}

	return nil
}

// SetUserDevice of the userDeviceToGeofence to the related item.
// Sets o.R.UserDevice to related.
// Adds o to related.R.UserDeviceToGeofences.
func (o *UserDeviceToGeofence) SetUserDevice(ctx context.Context, exec boil.ContextExecutor, insert bool, related *UserDevice) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"devices_api\".\"user_device_to_geofence\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_device_id"}),
		strmangle.WhereClause("\"", "\"", 2, userDeviceToGeofencePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.UserDeviceID, o.GeofenceID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserDeviceID = related.ID
	if o.R == nil {
		o.R = &userDeviceToGeofenceR{
			UserDevice: related,
		}
	} else {
		o.R.UserDevice = related
	}

	if related.R == nil {
		related.R = &userDeviceR{
			UserDeviceToGeofences: UserDeviceToGeofenceSlice{o},
		}
	} else {
		related.R.UserDeviceToGeofences = append(related.R.UserDeviceToGeofences, o)
	}

	return nil
}

// UserDeviceToGeofences retrieves all the records using an executor.
func UserDeviceToGeofences(mods ...qm.QueryMod) userDeviceToGeofenceQuery {
	mods = append(mods, qm.From("\"devices_api\".\"user_device_to_geofence\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"devices_api\".\"user_device_to_geofence\".*"})
	}

	return userDeviceToGeofenceQuery{q}
}

// FindUserDeviceToGeofence retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindUserDeviceToGeofence(ctx context.Context, exec boil.ContextExecutor, userDeviceID string, geofenceID string, selectCols ...string) (*UserDeviceToGeofence, error) {
	userDeviceToGeofenceObj := &UserDeviceToGeofence{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"devices_api\".\"user_device_to_geofence\" where \"user_device_id\"=$1 AND \"geofence_id\"=$2", sel,
	)

	q := queries.Raw(query, userDeviceID, geofenceID)

	err := q.Bind(ctx, exec, userDeviceToGeofenceObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from user_device_to_geofence")
	}

	if err = userDeviceToGeofenceObj.doAfterSelectHooks(ctx, exec); err != nil {
		return userDeviceToGeofenceObj, err
	}

	return userDeviceToGeofenceObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *UserDeviceToGeofence) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no user_device_to_geofence provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(userDeviceToGeofenceColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	userDeviceToGeofenceInsertCacheMut.RLock()
	cache, cached := userDeviceToGeofenceInsertCache[key]
	userDeviceToGeofenceInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			userDeviceToGeofenceAllColumns,
			userDeviceToGeofenceColumnsWithDefault,
			userDeviceToGeofenceColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(userDeviceToGeofenceType, userDeviceToGeofenceMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(userDeviceToGeofenceType, userDeviceToGeofenceMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"devices_api\".\"user_device_to_geofence\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"devices_api\".\"user_device_to_geofence\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into user_device_to_geofence")
	}

	if !cached {
		userDeviceToGeofenceInsertCacheMut.Lock()
		userDeviceToGeofenceInsertCache[key] = cache
		userDeviceToGeofenceInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the UserDeviceToGeofence.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *UserDeviceToGeofence) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	userDeviceToGeofenceUpdateCacheMut.RLock()
	cache, cached := userDeviceToGeofenceUpdateCache[key]
	userDeviceToGeofenceUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			userDeviceToGeofenceAllColumns,
			userDeviceToGeofencePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update user_device_to_geofence, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"devices_api\".\"user_device_to_geofence\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, userDeviceToGeofencePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(userDeviceToGeofenceType, userDeviceToGeofenceMapping, append(wl, userDeviceToGeofencePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update user_device_to_geofence row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for user_device_to_geofence")
	}

	if !cached {
		userDeviceToGeofenceUpdateCacheMut.Lock()
		userDeviceToGeofenceUpdateCache[key] = cache
		userDeviceToGeofenceUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q userDeviceToGeofenceQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for user_device_to_geofence")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for user_device_to_geofence")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o UserDeviceToGeofenceSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userDeviceToGeofencePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"devices_api\".\"user_device_to_geofence\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, userDeviceToGeofencePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in userDeviceToGeofence slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all userDeviceToGeofence")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *UserDeviceToGeofence) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no user_device_to_geofence provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(userDeviceToGeofenceColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	userDeviceToGeofenceUpsertCacheMut.RLock()
	cache, cached := userDeviceToGeofenceUpsertCache[key]
	userDeviceToGeofenceUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			userDeviceToGeofenceAllColumns,
			userDeviceToGeofenceColumnsWithDefault,
			userDeviceToGeofenceColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			userDeviceToGeofenceAllColumns,
			userDeviceToGeofencePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert user_device_to_geofence, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(userDeviceToGeofencePrimaryKeyColumns))
			copy(conflict, userDeviceToGeofencePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"devices_api\".\"user_device_to_geofence\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(userDeviceToGeofenceType, userDeviceToGeofenceMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(userDeviceToGeofenceType, userDeviceToGeofenceMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert user_device_to_geofence")
	}

	if !cached {
		userDeviceToGeofenceUpsertCacheMut.Lock()
		userDeviceToGeofenceUpsertCache[key] = cache
		userDeviceToGeofenceUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single UserDeviceToGeofence record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *UserDeviceToGeofence) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no UserDeviceToGeofence provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), userDeviceToGeofencePrimaryKeyMapping)
	sql := "DELETE FROM \"devices_api\".\"user_device_to_geofence\" WHERE \"user_device_id\"=$1 AND \"geofence_id\"=$2"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from user_device_to_geofence")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for user_device_to_geofence")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q userDeviceToGeofenceQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no userDeviceToGeofenceQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from user_device_to_geofence")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for user_device_to_geofence")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o UserDeviceToGeofenceSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(userDeviceToGeofenceBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userDeviceToGeofencePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"devices_api\".\"user_device_to_geofence\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userDeviceToGeofencePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from userDeviceToGeofence slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for user_device_to_geofence")
	}

	if len(userDeviceToGeofenceAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *UserDeviceToGeofence) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindUserDeviceToGeofence(ctx, exec, o.UserDeviceID, o.GeofenceID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UserDeviceToGeofenceSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := UserDeviceToGeofenceSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userDeviceToGeofencePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"devices_api\".\"user_device_to_geofence\".* FROM \"devices_api\".\"user_device_to_geofence\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userDeviceToGeofencePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in UserDeviceToGeofenceSlice")
	}

	*o = slice

	return nil
}

// UserDeviceToGeofenceExists checks if the UserDeviceToGeofence row exists.
func UserDeviceToGeofenceExists(ctx context.Context, exec boil.ContextExecutor, userDeviceID string, geofenceID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"devices_api\".\"user_device_to_geofence\" where \"user_device_id\"=$1 AND \"geofence_id\"=$2 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, userDeviceID, geofenceID)
	}
	row := exec.QueryRowContext(ctx, sql, userDeviceID, geofenceID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if user_device_to_geofence exists")
	}

	return exists, nil
}

// Exists checks if the UserDeviceToGeofence row exists.
func (o *UserDeviceToGeofence) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return UserDeviceToGeofenceExists(ctx, exec, o.UserDeviceID, o.GeofenceID)
}
